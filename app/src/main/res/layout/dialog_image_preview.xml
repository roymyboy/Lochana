<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:background="@drawable/preview_dialog_background"
    android:padding="16dp">

    <FrameLayout
        android:id="@+id/imageFrame"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:background="@drawable/chat_image_background"
        android:clipToOutline="true">

        <com.lochana.app.ui.ZoomableImageView
            android:id="@+id/previewImage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/chat_image_preview_content_description" />

        <ImageButton
            android:id="@+id/previewClose"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_gravity="end|top"
            android:layout_margin="8dp"
            android:background="@drawable/preview_close_background"
            android:contentDescription="@string/chat_image_preview_close"
            android:padding="6dp"
            android:src="@drawable/ic_close"
            app:tint="@android:color/white" />
    </FrameLayout>

    <TextView
        android:id="@+id/previewDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        android:fontFamily="sans-serif-medium"
        android:gravity="start" />

</LinearLayout>